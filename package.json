{
  "name": "solid",
  "version": "1.0.0",
  "description": "Project with SOLID principles.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "fastify start -w -l info -P src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^6.0.0",
    "@fastify/sensible": "^6.0.0",
    "fastify": "^5.0.0",
    "fastify-cli": "^7.0.0",
    "fastify-plugin": "^5.0.0",
    "joi": "^17.13.3"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": "20"
  }
}